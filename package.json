{
  "name": "extension",
  "version": "1.0.0",
  "description": "This project contains: - `extension/` — Chrome extension (manifest + popup UI + background script) - `backend/` — Python Flask backend that talks to an AI model and AnkiConnect",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/genai": "^1.28.0",
    "@google/generative-ai": "^0.24.1"
  }
}
